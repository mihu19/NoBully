python -m uvicorn brain.api_server:app --reload --host 127.0.0.1 --port 8000

wait until "Application startup complete."

for user:
install extension in browser
- go to extension page
- enable developer mode
- load unpacked
- add the /extensions folder

ask me for the API url (or make your own using ngrok)
on the extension UI paste the API url (ngrok url append /analyze)
enjoy being censored